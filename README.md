# SkillSwap Local

- Node.js + Express backend 
- React frontend with a rich, gradient UI

## How to run

```bash
cd backend
npm install
npm run dev

cd frontend
npm install
npm start
```

Backend runs on http://localhost:5000
Frontend runs on http://localhost:3000
