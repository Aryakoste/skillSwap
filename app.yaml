runtime: nodejs20
env: standard

# Free tier configurations
instance_class: F1

automatic_scaling:
  max_instances: 1 # Keep it to 1 instance to stay well within the generous free quota and avoid DB locks

env_variables:
  NODE_ENV: production
  CLIENT_URL: https://project-4f5a884c-3763-4127-947.uc.r.appspot.com
  DB_SOCKET_PATH: /cloudsql/project-4f5a884c-3763-4127-947:us-central1:skillswapmysql
  DB_USER: root
  DB_NAME: skillswap

handlers:
  - url: /.*
    script: auto
